# ГидроСтатистика 2026 — Техническое описание (архитектура)

---

## 1. Архитектура приложения

```
ГидроСтатистика_2026/
├── gui/
│   ├── main_window.py          ← Главное окно, QListWidget + QStackedWidget
│   ├── widget_work1.py         ← Норма годового стока
│   ├── widget_work2.py         ← Внутригодовое распределение
│   ├── widget_work3.py         ← Минимальный сток
│   ├── widget_work4.py         ← Максимальный сток (QTabWidget, голубой)
│   ├── widget_work5.py         ← Ледовые явления
│   ├── widget_work6.py         ← Водный баланс
│   ├── widget_work7.py         ← Рацион + IDF + Гидрографы (QTabWidget, фиолетовый)
│   ├── widget_work8.py         ← FDC + Регрессии + Статистика (QTabWidget, зелёный)
│   ├── widget_work9.py         ← ППУ + ГВП + Регулирование (QTabWidget, оранжевый)
│   └── widget_work10.py        ← Экология + Базовый сток (QTabWidget, красный)
│
├── core/
│   ├── gts_reference.py        ← Справочник классов ГТС (I–IV)
│   ├── hydraulics.py           ← Гидравлические расчёты
│   ├── profile.py              ← Профильные расчёты
│   │
│   ├── stats/                  ← Статистические модули
│   │   ├── data_loader.py      ← Загрузка данных из Excel
│   │   ├── frequency.py        ← Кривые обеспеченности (Пирсон III, К-М)
│   │   ├── parameters.py       ← Расчёт Qср, Cv, Cs, ε
│   │   ├── missing_data.py     ← Заполнение пропусков
│   │   ├── homogeneity.py      ← Тесты однородности (Диксон, Граббс)
│   │   ├── trends.py           ← Анализ трендов (Манн-Кендалл, Pettitt)
│   │   ├── gts_integration.py  ← Кривая с точками ГТС
│   │   ├── composite_curves.py ← Составные кривые
│   │   ├── series_extension.py ← Удлинение рядов
│   │   ├── report_export.py    ← Экспорт TXT/Excel
│   │   ├── kritsky_tables.py   ← Табличные значения Крицкого-Менкеля
│   │   ├── critical_values.py  ← Критические значения Диксона/Граббса
│   │   ├── flow_duration.py    ← Кривая длительностей (FDC)
│   │   ├── advanced_frequency.py ← MLE, L-моменты, GEV, PDS, диагностика
│   │   ├── confidence_bands.py ← Доверительные полосы кривых
│   │   ├── baseflow.py         ← Фильтрация базового стока
│   │   ├── spectral.py         ← FFT, экспонента Хёрста
│   │   └── drought.py          ← SPI, SPEI индексы засухи
│   │
│   └── hydrorash/              ← Гидрологические расчёты
│       ├── utils.py            ← Модульные коэффициенты, автокорреляция
│       ├── hydrological_periods.py ← Водохозяйственный год (НЛП, ЛП, ЛС)
│       ├── intra_annual.py     ← Внутригодовой ход стока
│       ├── minimal_runoff.py   ← Минимальный сток (СП 33 п.7)
│       ├── max_runoff.py       ← Максимальный сток (СП 33 п.8)
│       ├── min_runoff_extended.py ← 7/10/30-суточные мин., экосистемный
│       ├── ice_phenomena.py    ← Ледовые явления (ледостав, толщина, заторы)
│       ├── water_balance.py    ← Водный баланс, испарение (Дальтон, Мещерский)
│       ├── rational_method.py  ← Метод рациона, IDF кривые
│       ├── flood_hydrograph.py ← Гидрографы паводков
│       ├── snowmelt.py         ← Прогноз таяния снега
│       ├── regional_regressions.py ← Регрессии для нелогометрических рек
│       ├── spillway.py         ← Пропускная способность ППУ
│       ├── backwater.py        ← Кривые подпора (ГВП)
│       ├── reservoir_regulation.py ← Многолетнее регулирование
│       ├── sedimentation.py    ← Накопление наносов
│       └── ecological_flow.py  ← Экологический сток (Тессман, ECOFRAME)
│
├── build.py                    ← Скрипт сборки PyInstaller
├── build_nuitka.py             ← Альтернативная сборка Nuitka
├── create_test_data.py         ← Генерация тестовых данных
├── create_unified_template.py  ← Создание шаблона Excel
├── run_from_excel.py           ← Запуск из Excel
├── run_profile_calc.py         ← Запуск профильных расчётов
├── run_stats_demo.py           ← Демо-запуск статистики
└── .gitignore                  ← Исключения Git
```

---

## 2. Интерфейс (GUI)

### Навигация

Главное окно использует **QSplitter** с горизонтальным разделением:
- **Левая панель** (~260 px): `QListWidget` со списком 16 модулей (горизонтальный текст)
- **Правая область**: `QStackedWidget` с содержимым модулей

При клике по пункту в QListWidget вызывается `_switch_page()`, который переключает текущую страницу QStackedWidget.

### Внутренние вкладки

Модули 4, 7, 8, 9, 10 используют `QTabWidget` для организации подразделов. Каждый QTabWidget стилизован уникальным CSS-градиентом:

| Модуль | Цвет | Градиент |
|--------|------|----------|
| Work4 | Голубой | #E3F2FD → #BBDEFB (неакт.), #1E88E5 → #0D47A1 (акт.) |
| Work7 | Фиолетовый | #F3E5F5 → #E1BEE7 (неакт.), #8E24AA → #4A148C (акт.) |
| Work8 | Зелёный | #E8F5E9 → #C8E6C9 (неакт.), #43A047 → #1B5E20 (акт.) |
| Work9 | Оранжевый | #FFF3E0 → #FFE0B2 (неакт.), #FB8C00 → #E65100 (акт.) |
| Work10 | Красный | #FFEBEE → #FFCDD2 (неакт.), #E53935 → #B71C1C (акт.) |

---

## 3. Зависимости

| Пакет | Версия | Назначение |
|-------|--------|-----------|
| PyQt6 | 6.5+ | GUI (QMainWindow, QSplitter, QListWidget, QStackedWidget, QTabWidget) |
| NumPy | 1.26+ | Числовые расчёты |
| Pandas | 2.1+ | Табличные данные |
| SciPy | 1.11+ | Статистика, оптимизация |
| Matplotlib | 3.8+ | Графики (FigureCanvasQTAgg) |
| openpyxl | 3.1+ | Чтение/запись Excel |
| PyInstaller | 6.21+ | Сборка exe |

---

## 4. Ключевые алгоритмы

### 4.1. Пирсон III типа (СП 33 п.6.4)

Плотность вероятности:

```
f(x) = (1/(α^β × Γ(β))) × (x−ε)^(β−1) × exp(−(x−ε)/α)
```

где:
- α = Cs²/4 — параметр формы
- β = 4/Cs² — параметр масштаба
- ε = Qср − 2×Cv×Qср/Cs — параметр положения

Квантили вычисляются через обратную функцию Гамма scipy:

```python
Q(P) = ε + α × gamma.ppf(1 - P, β)
```

### 4.2. Cornish-Fisher (аппроксимация)

```
x(P) = z(P) + (1/6)×(z²−1)×Cs + (1/24)×(z³−3z)×(Cs²−3×(Ku−3)) − (1/36)×(2z³−5z)×Cs³
```

где z(P) — квантиль нормального распределения, Ku — эксцесс.

### 4.3. Поправка на автокорреляцию (СП 33 п.6.2.4)

```
Cv_испр = Cv × √((1 + r₁) / (1 - r₁ × (n-2)/(n-1)))
```

где r₁ — коэффициент автокорреляции первого порядка.

### 4.4. Тест Диксона (выбросы)

```
Q_крит = |x_{i+1} - x_i| / (x_{max} - x_{min})
```

Сравнивается с табличным значением для n наблюдений и уровня значимости α=0.05.

### 4.5. Метод Риппла (многолетнее регулирование)

```
V_необ = max[ Σ_{i=1}^{k} (Q_i - Q_забор) ] для всех k = 1..n
```

Минимальный объём водохранилища, при котором забор Q_забор гарантирован на 100% времени.

### 4.6. Кривая подпора (метод последовательных сечений)

```
Δx = (E₂ - E₁) / (I - S̄_f)

где:
E = h + V²/(2g) — энергетический напор
S_f = Q²n²/(ω²R^(4/3)) — уклон трения (Маннинг)
```

### 4.7. Степень таяния снега (СП 33 п.8.1)

```
M_i = A × T_air,i × Δt (при T_air > 0)

где A — коэффициент таяния (2.0–4.0 мм/сут при +10°С)
```

### 4.8. Метод рациона (СП 33 п.8.3)

```
Q = q × F × α / 3.6

IDF: i = A × T^m / (t + B)^n
```

### 4.9. Сезонный Тессман (СП 32 прил. 8)

```
Q_эк_i = α_i × Q_ср × (Q_i / Q_ср)^β_i

где:
α_i — доля экостока от среднего расхода для месяца i
β_i — параметр чувствительности (0.5–1.0)
```

### 4.10. Экспонента Хёрста (R/S анализ)

```
H = lim_{n→∞} log(R_n / S_n) / log(n)

H > 0.5 — persistentный (долгосрочная память)
H = 0.5 — случайное блуждание
H < 0.5 — антипersistentный
```

---

## 5. Таблицы и справочники

### 5.1. Классы ГТС (СП 58 п.6)

| Класс | Паводок (P_осн) | Межень (P_осн) | Паводок (P_провер) | Межень (P_провер) |
|-------|-----------------|----------------|---------------------|---------------------|
| I | 0.1% (T=1000) | 99% | 0.01% (T=10000) | 99.9% |
| II | 0.5% (T=200) | 98% | 0.1% (T=1000) | 99.5% |
| III | 1% (T=100) | 95% | 0.5% (T=200) | 99% |
| IV | 2% (T=50) | 90% | 1% (T=100) | 97% |

### 5.2. IDF-параметры по климатическим зонам

| Зона | A | B | n | m |
|------|---|---|---|---|
| Арктика | 0.025 | 5.0 | 0.70 | 0.10 |
| Центральная | 0.050 | 10.0 | 0.78 | 0.13 |
| Юг | 0.065 | 12.0 | 0.80 | 0.15 |
| Кавказ | 0.080 | 15.0 | 0.82 | 0.17 |
| Вост. Сибирь | 0.035 | 6.0 | 0.72 | 0.11 |
| Дальний Восток | 0.060 | 10.0 | 0.76 | 0.14 |

### 5.3. Степень таяния (СП 33)

| Зона | A (мм/сут при +10°С) |
|------|----------------------|
| Арктика | 2.5 |
| Зап. Сибирь | 3.0 |
| Центральная | 3.5 |
| Юг | 4.0 |
| Кавказ | 3.5 |
| Вост. Сибирь | 2.8 |
| Дальний Восток | 3.2 |

### 5.4. Классы экологического режима (СП 32)

| Класс | Название | Q/Qср |
|-------|----------|-------|
| I | Заморный | ≤0.05 |
| II | Экстремально низкий | ≤0.10 |
| III | Низкий | ≤0.20 |
| IV | Оптимально низкий | ≤0.30 |
| V | Оптимальный | ≤0.50 |
| VI | Высокий | ≤0.80 |

---

## 6. Структура данных

### Входные данные (Excel)

| Колонка | Тип | Описание |
|---------|-----|----------|
| year | int | Год наблюдений |
| value | float | Расход (м³/с) или уровень (м) |
| post | str | Название поста (опционально) |
| month | int | Месяц 1–12 (для суточных) |
| day | int | День 1–31 |

### Выходные данные

| Формат | Описание |
|--------|----------|
| DataFrame (Pandas) | Таблицы с результатами |
| Figure (Matplotlib) | Графики |
| TXT отчёт | Текстовый отчёт по СП 482 |
| Excel отчёт | Книга с листами «Параметры», «Кривая», «ГТС» |

---

## 7. Точность и ограничения

| Параметр | Точность | Ограничение |
|----------|----------|-------------|
| Qср | ±0.1% | Требуется n ≥ 15 (СП 482) |
| Cv | ±5–15% | Зависит от n и формы ряда |
| Cs | ±20–40% | Ненадёжно при n < 20 |
| ε | ±1–10% | Рекомендуется ε < 10% |
| IDF | Региональная | Аппроксимация для 9 зон |
| ГВП | ±5–10% | Зависит от грубости сечений |

---

## 8. Сборка

### Из исходников (PyInstaller)

```bash
pip install pyinstaller
python build.py
```

### Альтернативная сборка (Nuitka)

```bash
pip install nuitka
python build_nuitka.py
```

### Сборка напрямую

```bash
python -X utf8 -m PyInstaller gui/main_window.py \
  --name=ГидроСтатистика_2026 \
  --onedir --windowed --clean --noconfirm \
  --icon=icon.ico \
  --hidden-import=PyQt6 --hidden-import=PyQt6.QtWidgets --hidden-import=PyQt6.QtGui \
  --hidden-import=pandas --hidden-import=numpy --hidden-import=scipy \
  --hidden-import=matplotlib --hidden-import=openpyxl \
  --hidden-import=matplotlib.backends.backend_qtagg \
  --hidden-import=core.hydrorash --hidden-import=core.hydrorash.utils \
  --hidden-import=core.hydrorash.hydrological_periods \
  --hidden-import=core.hydrorash.intra_annual \
  --hidden-import=core.hydrorash.minimal_runoff \
  --hidden-import=core.hydrorash.max_runoff \
  --hidden-import=core.hydrorash.ice_phenomena \
  --hidden-import=core.hydrorash.water_balance \
  --hidden-import=core.hydrorash.min_runoff_extended \
  --hidden-import=core.hydrorash.rational_method \
  --hidden-import=core.hydrorash.flood_hydrograph \
  --hidden-import=core.hydrorash.snowmelt \
  --hidden-import=core.hydrorash.regional_regressions \
  --hidden-import=core.hydrorash.spillway \
  --hidden-import=core.hydrorash.backwater \
  --hidden-import=core.hydrorash.reservoir_regulation \
  --hidden-import=core.hydrorash.sedimentation \
  --hidden-import=core.hydrorash.ecological_flow \
  --hidden-import=core.stats.flow_duration \
  --hidden-import=core.stats.advanced_frequency \
  --hidden-import=core.stats.confidence_bands \
  --hidden-import=core.stats.baseflow \
  --hidden-import=core.stats.spectral \
  --hidden-import=core.stats.drought \
  --hidden-import=gui.widget_work1 --hidden-import=gui.widget_work2 \
  --hidden-import=gui.widget_work3 --hidden-import=gui.widget_work4 \
  --hidden-import=gui.widget_work5 --hidden-import=gui.widget_work6 \
  --hidden-import=gui.widget_work7 --hidden-import=gui.widget_work8 \
  --hidden-import=gui.widget_work9 --hidden-import=gui.widget_work10
```

### Результат

```
dist/
└── ГидроСтатистика_2026/
    ├── ГидроСтатистика_2026.exe
    └── ... (библиотеки)
```

### Размер сборки

~250–300 МБ (включая Python, PyQt6, NumPy, Pandas, SciPy, Matplotlib)

---

## 9. Репозиторий

- **URL:** https://github.com/maksimnelson356-sudo/Hydrology
- **Ветка:** main
- **Файлы в .gitignore:** `__pycache__/`, `build/`, `dist/`, `*.xlsx`, `*.docx`, `*.spec`, `*.ico`, `main_window.dist/`

---

*Документ составлен для версии 2026.1.0*
*Последнее обновление: Июль 2026*
